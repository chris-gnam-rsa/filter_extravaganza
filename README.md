# Filter Extravaganza
Where in the world is Matt ~~Lauer~~ Gorban?

## Setup the environment:

```py
conda env create -f environment.yml
conda activate environment.yml
```

If your environment is out of date, simply activate it and run:

```py
conda env update -f environment.yml
```

## Run Example:

```py
python simulate.py
```